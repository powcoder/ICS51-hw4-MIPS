https://powcoder.com
代写代考加微信 powcoder
Assignment Project Exam Help
Add WeChat powcoder
.text

validKingMove:

	# COMMNET/UNCOMMENT THE LINE FOR THE CASE YOU WANT TO TEST

	li $v0, -2  #for error with input arguments (invalid values for from, to, player)
	#li $v0, -1  # if the move is invalid
	#li $v0, 0   # if the move is valid and unobstructed.
	#li $v0, 1   #if the move is valid and a piece is captured.

	jr $ra

validQueenMove:
	# COMMNET/UNCOMMENT THE LINE FOR THE CASE YOU WANT TO TEST

	li $v0, -2  #for error with input arguments (invalid values for from, to, player)
	#li $v0, -1  # if the move is invalid
	#li $v0, 0   # if the move is valid and unobstructed.
	#li $v0, 1   #if the move is valid and a piece is captured.

	jr $ra

validKnightMove:
	# COMMNET/UNCOMMENT THE LINE FOR THE CASE YOU WANT TO TEST

	li $v0, -2  #for error with input arguments (invalid values for from, to, player)
	#li $v0, -1  # if the move is invalid
	#li $v0, 0   # if the move is valid and unobstructed.
	#li $v0, 1   #if the move is valid and a piece is captured.

	jr $ra

validPawnMove:
	# COMMNET/UNCOMMENT THE LINE FOR THE CASE YOU WANT TO TEST

	li $v0, -2  #for error with input arguments (invalid values for from, to, player)
	#li $v0, -1  # if the move is invalid
	#li $v0, 0   # if the move is valid and unobstructed.
	#li $v0, 1   #if the move is valid and a piece is captured.

	jr $ra